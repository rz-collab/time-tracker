A minimal compact window count up time tracker that allows naming task and keeps a list of task completed with their durations. It's fully vibe coded.

Runs installer that creates Desktop and Start Menu shortcut. 
```bash
npm install
npm run build
``` 

You can find it in `C:\Users\<YourName>\AppData\Local\Programs\Time Tracker\`
